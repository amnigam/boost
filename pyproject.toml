[project]
name = "boost"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.27.0",
]

[tool.setuptools]
py-modules = ["main", "package_input_parser"]

[project.scripts]
boost-scan = "main:cli"
